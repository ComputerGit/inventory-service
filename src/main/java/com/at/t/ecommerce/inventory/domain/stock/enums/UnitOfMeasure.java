package com.at.t.ecommerce.inventory.domain.stock.enums;

public enum UnitOfMeasure {
	EACH,       // Discrete items (Phones, Cases)
    KILOGRAM,   // Weight (Bulk shipping material)
    METER,      // Length (Cabling)
    LITER       // Volume (Cooling fluid)
}
