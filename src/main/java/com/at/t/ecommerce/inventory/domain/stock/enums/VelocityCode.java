package com.at.t.ecommerce.inventory.domain.stock.enums;

public enum VelocityCode {
	A_FAST_MOVER,  // High demand (Keep near shipping dock)
    B_STANDARD,    // Normal demand
    C_SLOW_MOVER   // Low demand (Store deep in warehouse)
}
